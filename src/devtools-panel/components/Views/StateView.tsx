// The UI from ../Content.tsx should be in here, however the trackDiffFrames should probably remain
// global. That probably wont happen until we move that data into a store because passing it through
// would be a pain. So for now this file is a placeholder.
